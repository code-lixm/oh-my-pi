搜索长期记忆中的相关信息。返回按相关性排序的原始匹配条目。

主动使用——在回答关于过去对话、用户偏好、项目决策或任何先前上下文有助于提高准确性的主题的问题之前使用。如有疑问，先回忆。

当你需要具体事实或条目时，优先使用 `recall`。当你需要跨多条记忆综合得出答案时，改用 `reflect`。

每个结果中的内容都是预览。末尾的 `…` 表示已截断（`truncated: true`、`full_length` 给出原始大小）。使用 `read memory://<id>` 获取完整行——这是进行任何 `memory_edit update` 之前的必需步骤。
