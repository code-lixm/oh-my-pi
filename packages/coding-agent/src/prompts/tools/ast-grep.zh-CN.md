通过语法匹配进行结构化代码搜索。

<instruction>
- 在语法形态比文本更重要时使用（调用、声明、语言结构）
- 将每次调用都限定为一种语言
- `pat` 是一个抽象语法树模式；不相关的模式要分开调用
- `$NAME` 捕获一个节点；`$_` 匹配一个但不绑定；`$$NAME` 捕获零个或多个；`$- 在语法形态比文本更重要时使用（调用、声明、语言结构）
- 将每次调用都限定为一种语言
- `pat` 是一个抽象语法树模式；不相关的模式要分开调用
- `$NAME` 捕获一个节点；`$_` 匹配一个但不绑定；`$$NAME` 捕获零个或多个； 匹配零个或多个但不绑定。使用 `$$NAME`，不要用 `$NAME`——双美元形式无效
- 元变量名要全大写，并且 MUST 是整个抽象语法树节点——像 `prefix$VAR`、`"hello $NAME"` 或 `a $OP b` 这样的部分文本不起作用
- 同一个元变量出现两次 → 两处出现 MUST 匹配完全相同的代码（`$A == $A` 匹配 `x == x`，不匹配 `x == y`）
- 模式 MUST 解析为单个有效的抽象语法树节点。非独立片段 → 用上下文包裹，例如 `class $_ { … }`
- C++ 表达式语句调用末尾需要 `;`：`ns::doThing($ARG);`，`$CALLEE($ARG);`
- TS declarations/methods——容忍未知注解：`async function $NAME($$ARGS): $_ { $$BODY }` 或 `class $_ { method($ARG: $_): $_ { $$BODY } }`
- 声明形式是不同的形态——`function foo`、方法 `foo()`、`const foo = () => {}`；先搜索正确形式，再下结论说不存在
- 最宽松的存在性检查：`pat: "executeBash"`，配合更窄的 `path`
</instruction>

<output>
- 在快照标签标题下的匹配：`[src/foo.ts#1A2B]`、`*42:` 已匹配、` 43:` 上下文
</output>

<critical>
- AVOID 仓库根目录扫描——先缩小 `path`
- 解析问题 = 查询失败，不是不存在：在断定“无匹配”之前，先修正模式或收紧 `path`
- 宽泛的跨子系统探索：你 SHOULD 先使用任务工具 + 侦察子代理
</critical>
