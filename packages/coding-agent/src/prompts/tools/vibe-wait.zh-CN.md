阻塞等待，直到某一个被监视的会话完成其当前轮次、超时时间结束，或你被中断——并非等到全部完成。要继续等待，请重新发出此请求。

轮次结果通常会自行送达；你 NEVER 需要这个来接收输出。仅当你被完全阻塞且无法引导任何其他会话时才使用它。

- `sessions` — 要监视的标识。省略则监视每个有进行中轮次的会话。
- `timeout` — 等待的秒数（默认 30）。

已完成轮次的完整结果（活动轨迹 + 响应）会在此处返回，且不会再单独重复送达。
