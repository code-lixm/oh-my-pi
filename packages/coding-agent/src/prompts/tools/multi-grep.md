Search file contents for ANY of several literal patterns with OR semantics.

<instruction>
- Use when several identifiers or naming variants must be found in one call.
- Include snake_case, camelCase, and PascalCase variants as separate `patterns`.
- Patterns are literal. Use `constraints` for file paths, globs, and exclusions.
- Use the returned cursor for another page.
</instruction>
