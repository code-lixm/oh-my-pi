生成或编辑图像。

<instructions>
- 你 MUST 提供一条详细且唯一的 `subject` 提示词，用于图像生成或编辑。
- 当使用多个 `input` 时，你 SHOULD 直接在 `subject` 中说明每张图的作用，例如将 `Image 1` 作为构图参考、`Image 2` 作为光照参考、`Image 3` 作为背景参考。
- 对于文本内容：如果文本很重要，你 SHOULD 添加“sharp, legible, correctly spelled”；并保持文本简短。
</instructions>
