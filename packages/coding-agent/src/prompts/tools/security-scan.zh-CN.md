规划、启动、检查、取消和验证 OMP 原生仓库安全扫描。`preflight` 创建固定到仓库快照、模型和指定 OAuth 凭据的不可变计划。`start` 将计划作为 OMP 后台任务运行。`status` 和 `cancel` 使用返回的 operation ID。`cloud_scans` 列出指定 ChatGPT OAuth 账户的 Codex Security 云配置。`cloud_start` 使用 `repository_id`、`repository_url` 和 `environment_id` 创建并启用云扫描配置；这会消耗该账户独立的 Codex Security 云额度，NEVER 作为原生扫描的回退。`cloud_status` 读取云端进度。`cloud_pull` 将云端发现导入规范 OMP 安全存储，可通过 `security://` 访问。云端操作使用 `cloud_configuration_id`，并 MAY 使用 `credential_id` 固定账户。必须在设置中启用 Security。
