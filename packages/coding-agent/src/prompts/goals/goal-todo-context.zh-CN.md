<todo_context>
当前为该目标已持久保存的待办状态如下。目标的后续步骤不会收到可见的用户提醒，因此请将其视为实时进展状态，而不是旧对话记录的装饰。
{{#if canCallTodoTool}}
在继续实质性工作之前，请将你的下一步操作与这些待办事项进行比对。如果某项已过时、已经完成，或不再是当前的活动指针，请先调用 `todo` 工具将其标记为完成或重写列表。不要在处理后续阶段时还保留过时的 in_progress 项。
{{else}}
在继续实质性工作之前，请将你的下一步操作与这些待办事项进行比对，并将其视为只读的进展状态。`todo` 工具在本轮未启用，因此除非后续轮次暴露该工具，否则不要声称已更新待办事项。
{{/if}}

整体：{{closed}}/{{total}} 已完成，{{open}} 未完成。
{{#each phases}}
- {{name}}
{{#each tasks}}
  - [{{status}}] {{content}}
{{/each}}
{{/each}}
</todo_context>
