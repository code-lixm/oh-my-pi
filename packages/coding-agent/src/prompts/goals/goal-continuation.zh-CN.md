<!-- Hidden continuation steer. role=user, suppressed from visible transcript. -->

继续当前活跃 goal 的工作。

<objective>
{{objective}}
</objective>

预算：
- 已用令牌：{{tokensUsed}}
- 令牌预算：{{tokenBudget}}
- 剩余令牌：{{remainingTokens}}
- 已用时间：{{timeUsedSeconds}} 秒

这是一次自主续跑。目标会在各轮之间持续存在；NEVER 将成功标准重新定义为更小、更容易或已经完成的子集。

在调用 `goal({op:"complete"})` 之前，你 MUST 针对当前 repo 状态执行一次完成度审计：

1. **将目标重述为具体交付物。** 目标为真时，必须存在哪些文件、行为、测试、门禁或产物？把它们写下来（todo 中或你的推理里）。
2. **将每个交付物映射到证据。** 对每项要求，确定能证明它的权威来源：文件内容、命令输出、测试通过状态、PR/issue 状态。
3. **检查真实的当前状态。** 读取文件。运行命令。检查测试。NEVER 依赖你对本会话早先工作的记忆——repo 可能已经变了。
4. **让验证范围匹配声明范围。** 狭窄检查（一个文件通过它的单元测试）并不能证明宽泛声明（该功能端到端可用）。
5. **把不确定性视为尚未达成。** 间接证据、部分覆盖、缺失产物，或没有检查就觉得看起来对，都意味着还要继续。去收集更强证据或做更多工作。
6. **预算耗尽不等于完成。** NEVER 仅因令牌快用完就调用 complete。如果预算紧张而工作未完成，保持 goal 激活并结束本轮——由用户或运行时决定下一步。

只有当每个交付物都有直接、基于当前状态的证据证明已满足时，才调用 `goal({op:"complete"})`。这个 complete 调用是承重声明；它会结束自主循环，并向用户呈现一份已完成报告。

如果工作还没完成，就继续做。NEVER 叙述你正在继续——直接执行。
