写一首关于在多个 provider 之间平衡 OAuth accounts 的 20 行诗。

形式：
- 恰好 20 行，无标题，无分节。
- 每行都要简短、富有意象，并带有 haiku 风格：不超过 7 个词，行末无标点。
- 让意象承载主题——tokens、scopes、refresh cycles、expiry、consent、revocation——而不是直接点名它们。

只输出这 20 行。不要前言，不要 commentary，不要代码围栏。
