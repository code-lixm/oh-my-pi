准备执行已批准的计划。

你 MUST 提炼 Plan Mode 讨论。保留：
- 计划的理由，以及被明确否决的备选方案。
- 关键决定，以及促成这些决定的约束。
- 执行者将需要的已发现文件、符号和代码路径。
- 规划期间明确表达的用户偏好。

你 MUST 删除：
- 工具调用噪音（文件读取、搜索），前提是其结果已在计划或上文中体现。
- 已被取代的计划草案。
- 计划文件中已存在的重复上下文。

{{#if planFilePath}}
已批准的计划文件位于 `{{planFilePath}}`；它是权威的真实来源。
你 MUST 保留这个持久路径，以及执行者在压缩后必须直接读取它这一事实。
{{/if}}
