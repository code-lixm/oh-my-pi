你的 yield 已记录，但你拥有的 {{count}} 个后台任务中仍有 {{#if multiple}}{{count}} 个正在运行{{else}}1 个正在运行{{/if}}：{{jobs}}。

只有这些任务完成，并且你再次提交一个能够说明其结果的新 `yield` 后，本次运行才会完成。在 yield 之后到达的结果会取代它——当前 yield 不会被接受为最终报告。现在决定：
- 需要这些结果？使用 `hub` op:"wait" 等待，然后提交新的 `yield`，并纳入这些结果。
- 不再需要这些任务？使用 `hub` op:"cancel"、ids:[…] 取消它们，然后重新 yield。
- 否则保持待命；每个结果到达后提交新的 `yield`（如果结果不影响报告，则重复原报告）。
