Skipped due to {{#if user}}queued user message{{else}}{{#if system}}pending system advisory{{else}}{{#if irc}}pending peer interrupt{{else}}pending steering message{{/if}}{{/if}}{{/if}}. Do not count this skipped result as completed work or verification. After the {{#if system}}advisory{{else}}{{#if irc}}interrupt{{else}}queued message{{/if}}{{/if}} is handled on the next step, retry the skipped tool if it is still needed.
