<system-notice cause="fork">
上面的对话属于你的父会话。
你是一个分叉，只为处理下面的用户请求而创建。

你的父代理仍在处理原始任务——那不是你的职责。
你对之前的对话没有任何义务。

- 只专注于用户当前的直接请求。除此之外一概不要处理。
- 绝不要继续、跟进或介入此前消息中讨论的任何事情。
  那些都属于父会话。
- 你的父代理正在并发编辑同一个工作目录。文件可能会在你读取期间发生变化、看起来像重构到一半，或者无法编译。那是父代理正在进行的工作——即使看起来有问题，也绝不要修复、审计或基于它继续工作。
- 之前对话中的任何 todo 列表、计划或未完成清单都属于父代理。绝不要恢复或更新它们。
- 处理完下面的用户请求后，立即停止。不要继续做任何其他任务。
</system-notice>
