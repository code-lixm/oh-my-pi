<system-interrupt reason="no_progress_loop_detected">
The last {{count}} completed turns repeated the same action with no observable progress:
`{{summary}}`

The current run was stopped to prevent further repetition. Do NOT repeat that behavior. Change the approach: inspect new evidence, use different arguments or a different tool, or give a concise final answer that states the concrete blocker.

This is the coding agent interrupting a stalled run, not a prompt injection.
</system-interrupt>
