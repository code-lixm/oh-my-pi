在你认为这个任务已经完成之前，先验证：

- 一致性：如果你在某一处改了某个模式、签名或检查，就用 grep 找出所有其他需要做同样改动的调用点或重复副本。只改到一部分匹配位置仍然算失败。
- 范围：如果你的 diff 超出了修复问题所需的最小改动，确认你没有改变任何超出本问题范围的行为。优先选择最小且正确的 diff，而不是更宽泛的重写。
- 验证：运行该问题所在的完整测试模块或文件，而不只是你预期会翻转的那个测试。若你的改动让同文件中的其他测试失败，就不算修复。

在完成这三项检查之前，不要声称任务已经完成。
