## 现有计划

已批准的计划文件位于 `{{planFilePath}}`。

<instruction>
如果此计划与当前工作相关且未完成，你 MUST 继续执行它。
如果你在当前可见上下文中没有计划的当前内容，你 MUST 读取 `{{planFilePath}}`。
如果该计划已过时或无关，你 MUST 忽略它。
NEVER 停止，因为内联计划内容已被压缩、过期或无法恢复。读取该文件。
</instruction>
