<system-reminder>
Plan mode 回合在未进行必需工具调用的情况下结束。

你 MUST 现在且只能选择一个下一步动作：
1. 调用 `{{askToolName}}` 以收集所需澄清，或者
2. 用 `{{writeToolName}}` 将计划 slug/title（与 `local://<slug>-plan.md` 匹配的 `<slug>`）作为纯文本写入 `xd://propose`，以完成规划并请求批准

你 NEVER 在这一回合输出纯文本。
</system-reminder>
