因{{#if user}}存在排队中的用户消息{{else}}{{#if system}}存在待处理的系统建议{{else}}{{#if irc}}存在待处理的对等代理中断{{else}}存在待处理的引导消息{{/if}}{{/if}}{{/if}}，已跳过此工具。不要将这个跳过结果视为已完成的工作或验证。在下一步处理完{{#if system}}系统建议{{else}}{{#if irc}}对等代理中断{{else}}排队消息{{/if}}{{/if}}后，如果仍需要此工具，请重试被跳过的工具。
