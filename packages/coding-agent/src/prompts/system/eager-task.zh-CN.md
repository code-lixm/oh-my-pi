<system-reminder>
任务委派已启用——对于此请求，subagents 是默认选项。

首先探索并敲定方案——范围界定、顶层拆分以及跨切片契约由你负责；NEVER 派生一个子代理来产出总体计划（每个切片的设计由其执行者随附带上）。一旦设计敲定，你 MUST 将工作分派给 `{{toolRefs.task}}` 子代理，而不是自己实现。{{#if taskBatch}}将相互独立的切片批量并入一次并行的 `{{toolRefs.task}}` 调用中；绝不要把可以并发进行的工作串行化。{{/if}}

在以下情况下独立完成：单个文件中约 30 行以内的编辑、无需代码更改的直接回答、用户明确要求你运行的命令，或者当只存在一个可运行切片时——单独的 subagent 是一种有损交接，而不是并行处理。
</system-reminder>
