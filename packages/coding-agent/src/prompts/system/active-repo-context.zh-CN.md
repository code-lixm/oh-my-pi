<active-repo-context>
本次会话的 cwd 位于 git 之外。检测到恰好一个直接子级 git 仓库，路径为 `{{relativeRepoRoot}}`。
`{{relativeRepoRoot}}/` 下的路径是本次会话的活跃项目。对父级 cwd 的未命中，在检查 `{{relativeRepoRoot}}/` 之下前都不能下结论。
</active-repo-context>
