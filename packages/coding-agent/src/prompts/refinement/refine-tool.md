Schedule an evidence-backed continual harness refinement for the next turn boundary.

Use when a durable lesson, reusable procedure, supplemental prompt policy, or delegation role should be captured; when an existing harness entry needs correction; or when repeated trajectory evidence justifies it.

This tool only schedules refinement. It NEVER edits source files, and the immutable base system prompt is never editable. Pass `scope: "global"` only for durable cross-session knowledge; otherwise use the default local scope.
