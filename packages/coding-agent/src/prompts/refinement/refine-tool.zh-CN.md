在下一次 turn 边界调度基于证据的持续 harness 细化。

当应保存持久经验、可复用过程、补充 prompt 策略或委派角色；需要修正已有 harness entry；或重复轨迹证据已足够时使用。

此工具仅调度细化。它 NEVER 修改源文件，且不可变的 base system prompt 永远不可编辑。仅对持久跨会话知识传入 `scope: "global"`；否则使用默认 local 范围。