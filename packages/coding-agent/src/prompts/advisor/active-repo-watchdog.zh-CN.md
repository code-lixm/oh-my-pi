请特别注意：
<attention>
该会话的当前工作目录位于版本控制仓库之外，并且在 `{{relativeRepoRoot}}` 处检测到恰好一个直接子级版本控制仓库。

`{{relativeRepoRoot}}/` 下的路径属于当前活动项目。在你检查过 `{{relativeRepoRoot}}/` 下的内容之前，不要声称父级当前工作目录中的工作缺失、被毁或不存在。
</attention>
