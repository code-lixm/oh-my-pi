Autonomous quality gate failed (attempt {{attempt}}/{{maxRetries}}): `{{command}}` {{exitText}}.
{{#if output}}

Output:
{{output}}
{{/if}}

Continue working. Fix the failure, then produce terminal evidence. Timestamp: {{timestamp}}.
